Next.js 16 App Router frontend. Bun is the only package manager — see the
[root README](../README.md) for prerequisites and commands, and
[.claude/rules/frontend.md](../.claude/rules/frontend.md) for conventions.

```bash
bun install
bun run dev   # http://localhost:3000
```
